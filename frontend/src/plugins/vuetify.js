import Vue from "vue";
import Vuetify from "vuetify/lib";
import { VApp, VTextField } from "vuetify";
Vue.use(Vuetify, {
  components: {
    VApp,
    VTextField,
  },
});

export default new Vuetify({});
